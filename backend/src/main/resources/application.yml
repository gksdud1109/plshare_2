spring:
  application:
    name: plshare-backend
  profiles:
    default: demo
    active: demo

server:
  port: 8080

logging:
  level:
    com.plshare: DEBUG

app:
  session-secret: ${APP_SESSION_SECRET:demo-session-secret-change-me}
  session-ttl-seconds: ${APP_SESSION_TTL_SECONDS:2592000}
