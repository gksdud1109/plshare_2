# Modules

이 디렉터리는 특정 AI가 멈추거나 토큰 한도에 걸려도 다른 AI가 이어받을 수 있도록 작업을 모듈 단위로 정의한다.

원칙:
- 모듈은 1~3시간 내 끝낼 수 있는 크기로 유지한다.
- 각 모듈은 입력, 출력, 완료 조건을 가진다.
- 각 모듈은 `주 담당`과 `대체 담당`을 가진다.

기준 문서:
- `module-registry.md`
- `handoff-template.md`
